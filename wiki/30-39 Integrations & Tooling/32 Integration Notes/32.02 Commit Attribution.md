# 32.02 Commit Attribution

## What and why

The owner permits `jack@pippett.me` in public commit metadata.
Use `johnpippett <jack@pippett.me>` for this repository.
The former generic email address linked commits to an unrelated GitHub account.

The correction changes the author and committer identities in 38 existing commits.
It preserves every file tree, commit message, author date, committer date, and relationship between commits.
Each corrected parent replaces the corresponding original parent.
These changes produce new commit IDs. They do not change research results.

The [commit map](../../../docs/history/commit-map-2026-09-17.tsv) records the original ID, corrected ID, and unchanged tree ID.
Original research receipts and manifests retain their recorded IDs.
Use the map before you check out an earlier project commit.
External source commit IDs are unchanged.

The last original research commit is `b5a8d4db31b290b40bcc19572e1544425f8a56a3`.
Its corrected ID is `fd5af4eeea1bb395c8dde78fb5dfbbda4d42f53f`.
The commit that adds this note follows the corrected research history.

## Verification

All 38 pairs have identical tree IDs and messages.
Their dates and parent relationships were checked before publication.
A private Git bundle preserves the original history. Its integrity check passed.
The unfinished local files were backed up separately.

Compare each pair with the commit map.
Check GitHub attribution on the corrected commits.
Check the repository identity with `git config --local user.email` and `git config --local user.name`.
Do not restore the former generic address.

## Rollback

The private bundle contains the original commits.
Use a separate checkout to inspect them without changing the current branch.
Any remote rollback must check the current remote ID before updating the branch.
Keep the commit map with research records that cite original IDs.
