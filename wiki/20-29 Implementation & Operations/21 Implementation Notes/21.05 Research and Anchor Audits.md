# Research and anchor audits

## What changed

Added a [recent research audit](../../../docs/research/recent-research-audit.md) covering five 2025–2026 author records.
Added an [anchor audit](../../../docs/research/anchor-audit.md) for labels, marginalia, and writing layers.
Added a [directionality method audit](../../../docs/research/directionality-method-audit.md) and [independent review](../../../docs/research/directionality-review.md).
Added an [Antidotarium source audit](../../../docs/research/antidotarium-source-audit.md).
Added an [image audit](../../../docs/research/otaly-image-audit.md) for three repeated `otaly` labels.
Added a [medical-image comparison](../../../docs/research/balneological-anchor-assessment.md) for f84r and nearby pages.
Added [layout coordinates](../../../docs/research/fluid-scene-layout.md) for scene units and nearby writing on f84r and f78v.
Added an [EVA run evidence note](../../../docs/research/eva-run-unitization-evidence.md) about repeated `e` and `i` forms.
Added [mixed-cycle theory](../../../docs/research/mixed-cycle-identifiability.md) for models that permit singleton and paired units.
Added a [mechanism assessment](../../../docs/research/next-mechanism-experiment-assessment.md) for a possible word-context prediction test.
Added a conditional plant-label recurrence check to the anchor audit.
Added the eligibility result for a ring and margin-column comparison.
Added the coverage result for a proposed drawing-gap pairing test.
Added a source check for a published anagram decoder and its code.
Added a partial-key coverage check on three source-selected candidates fixed before extraction.

## Why

These records separate structural claims from plaintext recovery.
A reversed n-gram score can reflect boundaries and estimation rules. It does not establish physical reading direction by itself.
Repeated glyph strings constrain plaintext equality only under a declared context-independent or same-state decoder.
The released Antidotarium text mixes source languages and editorial material. It is not accepted as a clean Latin reference.

## Verification

The audit uses versioned author records, primary catalogues, and source-code inspection.
The directionality review confirms the reversal identities and the finite boundary counterexample.
Notebook metadata does not establish the requested historical version. The audit keeps that limit explicit.
The Antidotarium archive matched its declared size and checksum. No downloaded code was executed.
These records do not establish a manuscript key, language, or translation.

The image audit records Yale source hashes, target rectangles, and source metadata.
Two inspected labels are consistent with their transcription entries. The third remains uncertain.
Review found a clipped target crop. A larger source region now contains the complete selected run.
The primary agent inspected both revised target regions.
These are AI image inspections. They do not certify glyph boundaries or plaintext meaning.

The medical-image review found no semantic anchor in the compared sources.
The sources identify known medical image-text relations but no corresponding Voynich caption or treatment.
Review also found that altered leaf order can separate matching text and images in a known bath manuscript.
The result remains a bounded source comparison. It does not reject medical or semantic interpretations of f84r.

The layout record uses full Yale image dimensions, hashes, and approximate pixel boxes.
Root image review found four bounds that cut visible scene or writing edges.
The revised boxes include those edges and state larger boundary uncertainty.
The layout does not establish which writing region describes which scene.
These spatial annotations do not identify captions, reading order, or meanings.

The EVA note distinguishes connected strokes, historical analysis compounds, and linguistic units.
It records no universal rule that turns every repeated `e` or `i` run into one sign.
The strict extractor preserves accepted spelling; it rejects uppercase and bracketed candidates.
It does not recover connectivity information omitted by an upstream source.

Review of the mixed-cycle proof corrected the distinction between arbitrary and perfect matchings.
The final conditions distinguish feasible models, empty admissible sets, observed classes, and full inventory capacity.
With enough plaintext symbols and singleton classes, structural fit alone can provide no discrimination.

The mechanism proposal uses the earlier word-bigram failure to define a narrower prediction question.
A finite-window similarity gain would test local word context. It would not identify a historical copying process.
The existing manuscript groups remain exploratory evidence, even under a new split.
These three notes contain no new manuscript fit or measurement.

The later plant-label check corrected an incomplete source search.
Fragments 212 and 213 have attached labels and reported counterparts on f18v and f23r.
Image review confirmed attachment but could not verify either complete glyph string.
Both image-verification conditions failed.
A separate source-conditional query was specified before either word search ran.
The two fixed readings have no exact match in accepted text on their target pages.
IT covers all 71 accepted tokens on f18v; the other target checks have incomplete coverage.
Another implementation confirmed the counts while reusing the parser.
The exact-reuse branch is stopped. It recovered no plant name or meaning.
The plan and full local result remain in `results/plant-label-recurrence-v1/`.
They are local research records, not published artifacts.

The ring and column check tested a proposed shared 17-sign order.
Primary descriptions and images support source entries, but not the individual sign units required by the plan.
The ring records contain 68 ZL fields and 69 IT fields. The column includes a compound group recorded as `air`.
The comparison stopped before template matching. No sign groups or replacement targets were introduced.
The plan and gate record remain in `results/ring-column-order-v1/`.

A separate source audit found no fixed decoder in the reviewed numerical-code proposal.
Its missing expansion and boundary rules prevent a source-faithful divisibility test.
The anagram paper also does not require one global symbol order in the original words.
No manuscript runs followed those reviews. Their local record is `results/semantic-anchor-check-v1/gallows-and-current-paper-review.md`.
The earlier private plant-search note now includes the correction for fragments 212 and 213.
These checks changed the next research actions. They recovered no plaintext.

The drawing-gap check asks about physical pairing, without assuming linguistic adjacency.
It differs from the published comparison of separate token populations before and after drawings.
The fixed primary representation supplied no variable matched cells in either transcription.
The raw representation supplied only two ZL targets and four IT targets.
Both sources failed the coverage condition fixed before extraction.
No model fit, score, or image selection followed. The branch is stopped without weaker controls.
The result, plan, and executed script remain in `results/drawing-gap-pairing-v1/`.
Readback verified the source, plan, script, and dependency hashes.
A separate AI task confirmed the selected rows, exclusions, grouping, matched cells, and coverage decision.
The frozen counting script uses assertions; its recorded invocation used ordinary Python without optimization.

The anagram-decoder check found no complete published Voynich key or frozen-key transfer test.
The examined code revision is `e86ef47f79fef9a68438260436050a37e29d4c45`.
The primary agent checked its file list, README, solver inputs, and the original paper.
Separate AI tasks checked the method specification and the printed Hebrew example.
External training files are necessary program inputs. The code package does not contain the reported experimental data.
No code from that repository was executed. No key fit or manuscript experiment followed.
The source record remains in `results/semantic-anchor-check-v1/gallows-and-current-paper-review.md`.
This is a stopped replication branch, not a finding about the manuscript's language.

The partial-key check fixed f1v, f25r, and f9v before text extraction.
A separate AI source task selected these candidates without the key or Voynich text.
Their botanical identifications remain conditional and disputed.
Neither transcription gives a fully covered opening word under the fourteen listed EVA units.
Each opening contains `c`, which is absent from every permitted unit.
A separate source-line check confirmed the entries without the parser.
The first run retained the leading `<%>` marker in the first-field value and failed the acceptance check.
The corrected run removes only that marker.
The original plan, script, and failed output are preserved beside the corrected record.
No name comparison, new assignment, or replacement target followed.
The branch is stopped. Records remain in `results/bax-partial-key-v1/`.

The Hannig source review stopped automatic replication before any manuscript experiment.
See the reading-rule check in `docs/research/anchor-audit.md` for the primary source, relevant pages, and access limits.
Review those source passages before changing this decision.

The gallows continuation comparison fixes four maps and conditions on the next visual unit and Currier class.
This avoids a new count of the already reported immediate follower difference.
Neither source met the fixed reference-coverage rules. No target-tail score or map ranking followed.
The local plan, script, and result remain in `results/gallows-continuation-v1/`.
The primary agent reviewed the source argument, verified source hashes, and ran source-free selection fixtures.

A separate calculation initially attached incomplete training counts to targets encountered earlier in manuscript order.
With all training records counted first, it matched all 121 target records and support counts.
The study directory retains both the failed and corrected verification records.
The primary plan and result did not change.
The result does not reject the broader abbreviation proposal.

The fixed-expansion experiment tests word-count equations modulo three, with an unknown fixed boundary contribution.
It permits separate vectors for Currier A and B and uses the existing visual and raw unit representations.
The final certificates for all eight systems have full column rank.
Six reach full rank from training alone. Only the two ZL-A tracks receive validation and test constraints.

The result forces zero expansion-length remainders modulo three. It does not recover digit values or test arbitrary-space codes.
Read `reports/TRIPLET_BOUNDARIES.md` for selection, assumptions, and limits.

The public coefficient certificates omit word transcriptions. Exact source references remain available for verification.
The local plan, freeze, script, and result remain in `results/triplet-residue-v1/`.
Source-free fixtures cover elimination, exhaustive small-kernel agreement, and word selection.
The primary agent also verified each public certificate determinant with exact rational arithmetic.

A separate AI calculation reproduced all eight selections, matrix hashes, ranks, nullspaces, and 195 certificate references.
It confirmed full rank with exact integer determinants and verified each kernel against all applicable rows.
It reused the parser, span extractor, unit representation, and physical-group split.
The local verification script and result remain with the study. The public report records their hashes.

The separate line-boundary experiment removes the word-closure assumption.
Spaces need not mark plaintext words. The model uses one shared space contribution and one shared line-ending contribution.
The plan and script were frozen before selection. The experiment reuses the strict line selector and exact elimination from earlier work.

All eight training matrices have full column rank. Validation and test constraints were not evaluated.
Every represented unit length must be divisible by three under this model.
Read `reports/LINE_TRIPLET_BOUNDARIES.md` for selection, model limits, and source hashes.
Local records remain in `results/line-triplet-residue-v1/`.

The public coefficient matrices have nonzero determinants modulo three, verified with exact rational arithmetic.
They contain 197 source-row references and no line text or word lists.
The branch stopped without new moduli, boundary locations, padding classes, or phase rules.

A separate AI calculation reproduced the selected source references, exclusions, matrix hashes, ranks, nullspaces, and all 197 certificate rows.
It used a separate line selector and exact integer determinants. It reused the parser, physical-group split, and unit representation.
The primary agent verified the receipt against the frozen result. The report records the verification script and result hashes.

The Schechter source audit fixes revision `71f2f3c91e9113d285ab21e024f1dd70c1f43c44`.
The executable method uses whole-word lookup, followed by another lookup after removing four specified compound signs.
Changing nonempty output meanings preserves coverage. This code property does not validate the assigned meanings.
The reverse-encoding example also starts from the assigned glossary vocabulary.
We stopped the sign-rule transfer branch without package execution or a manuscript experiment.

The primary agent checked the decoder, reproduction guide, and reverse-encoding report.
A separate AI task checked the decoder and glossary-expansion tools.
The twelve source files and their hashes remain in `results/schechter-decoder-audit-v1/`.
Use the immutable source links in `docs/research/anchor-audit.md` to check the code and claim limits.

A named-witness image check examined four illustrated pages from Cod. Bodmer 135.
The primary agent inspected two of those pages. The existing f84r anchor decision remains unchanged.
The limited sample supplies no fixed caption or name. No text comparison followed.
Read `docs/research/balneological-anchor-assessment.md` for the source and limits.

## Rollback

Correct a disputed source claim in a new commit. Retain the cited version and the reason for correction.
Do not replace uncertainty with a guessed reading.
